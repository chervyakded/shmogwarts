#Берем родительский образ php:7.4-fpm
FROM php:8.1.0-fpm
#Открываем 9000 порт
EXPOSE 9000
# Устанавливаем рабочим каталогом контейнера /var/www/
WORKDIR /var/www/
#Копируем содержимое хостового каталога www в директорию var/www/
COPY ./www/ /var/www/
#Выполняем команду php-fpm
CMD ["php-fpm"]